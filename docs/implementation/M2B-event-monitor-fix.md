# Historical M2-B Experiment

This event-monitor approach was replaced and is not part of the active implementation.

`FlybyNighterApp` now uses a native AppKit window with the shared `MacKeyboardGameView` adapter. Current validation status is recorded in `M2BC-validation.md`.
